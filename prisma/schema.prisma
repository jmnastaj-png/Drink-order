// Prisma schema (SQLite for local dev). Replace datasource with Postgres in production.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Restaurant {
  id    String @id @default(cuid())
  name  String
  tables Table[]
  items  MenuItem[]
}

model Table {
  id    String @id @default(cuid())
  code  String @unique
  name  String
  restaurantId String
  restaurant Restaurant @relation(fields: [restaurantId], references: [id])
}

model MenuItem {
  id    String @id @default(cuid())
  name  String
  price Int
  restaurantId String
  restaurant Restaurant @relation(fields: [restaurantId], references: [id])
}

model Order {
  id    String @id @default(cuid())
  tableCode String
  total Int
  status String
  createdAt DateTime @default(now())
  items OrderItem[]
}

model OrderItem {
  id String @id @default(cuid())
  order Order @relation(fields: [orderId], references: [id])
  orderId String
  name String
  price Int
}
